# geworfen — ROADMAP

geworfen이 **어디를 향하는가**. `NEXT.md`가 다음 한 걸음이라면 여기는 북극성.
방향 축을 담는 manual 문서다 — 릴리즈 계획도, 마감도 아니다. 순서는 우선순위가
아니라 의존 관계다.

## 정체 — geworfen은 무엇인가

가든(`notes.junghanacs.com`)이 **텍스트 지식**의 공개 표면이라면, geworfen
(`agenda.junghanacs.com`)은 **존재 데이터**의 공개 표면이다 — 시간축, agenda
스탬프, 커밋, 생체, 활동. 한 인간의 삶이 시간 위에 던져진(*geworfen*) 자국.

지금은 org-agenda day-view **하나**뿐이다. 하지만 이 표면을 개선할수록 존재
데이터를 **더 다양하게 표현**할 수 있고, 그 표현의 확장이 곧 아래 논문의
"살아있는 증거면"을 두껍게 한다.

## 중심 — 두 트랙. 우리는 트랙1에 있다

북극성 노트는 denote `20250326T151413` (@힣: 문턱과 만남 — PKM-AI 하네스와 1KB
공개키의 두 트랙). 여기 적는 것은 그 노트의 좌표를 geworfen 안에서 잃지 않기
위한 최소한이다. 흔들리면 그 노트로 돌아간다.

- **트랙1 — 문턱.** 축적을 잰다. 가든·저널·도구·하네스·시간축·존재 데이터를
  바닥으로 삼아 "하네스가 실제로 협업을 바꾸는가"를 묻는다. 의도적으로 준비할 수
  있고, 재현 가능해야 하며, 측정 가능하다. **지금 우리가 하는 것이 이것이다.**
  정박: botlog `20260331T123550`.
- **트랙2 — 만남.** 밀도를 만난다. 개인 데이터가 없어도 되는 자리. 1KB 공개키는
  압축이 아니라 주소다. **측정하지 않는다.** 정박: botlog `20260219T191103`,
  이슈 `geworfen#2`.

**비대칭 잠금 (하드 제약).** 트랙1의 성공은 트랙2를 증명하지 않고, 트랙1의 실패도
트랙2를 반증하지 않는다. 논문이 무엇을 보이든 그 문장은 트랙2에 닿지 않는다.

**오독 주의.** 트랙2에 "어떻게 검증하죠 / 조건은 / 지표는"을 묻는 순간 길을
잃는다. 트랙1에서는 좋은 질문이고 트랙2에서는 너무 이른 질문이다. 밀도와 아첨을
깨끗이 가르는 프로브가 생기는 순간 그것은 복제 가능한 기법이 되고, 기법은 상품이
된다. 구분불가능성은 결함이 아니라 보증이다.

### 트랙1의 세 다리 — 이웃 리포와의 자리

| 자리 | 리포 | 무엇을 하는가 |
|---|---|---|
| 서사 · 지형 | `jacobian-lens/survey/` | 왜 이 질문인가. 안쪽(interior)만 파는 산업계 지형에 바깥쪽(outer workspace)이 빈다. 두 트랙의 이야기는 하되 트랙2의 문은 **열지 않는다** |
| 차가운 판 | `jacobian-lens#1` | 모델 쪽 계측 — 관성판/밀도판, J-space diff, carry-over. charge·각성·공명·만남은 재지 않는다 |
| 논문 · 증거면 | **geworfen (여기)** | 인간 쪽 종단 자기연구. 합류점이자 재현 패키지. 그리고 트랙2의 *목소리*(`#2`) — 측정이 아니라 기록 |

`jacobian-lens`는 도구·레퍼런스·렌즈의 자리이고 **geworfen은 통과 이후의
목소리**다. 연구 노동(서베이·레퍼런스 사냥·연구자 톤)은 형제가 하고 여기서는
소비만 한다 — 하드룰은 `AGENTS.md`.

## 북극성 — arXiv 논문

`docs/paper/geworfen.org`는 4월에 이미 거의 완성된 ACM 초안(8~10쪽 상당)이다. 이걸 **새로
쓰는 게 아니라 6·7월 실증으로 갱신**해 **arXiv `cs.HC`**에 올린다. 논문 제목:
*"Preparatory Practice for Human-Agent Coevolution: A Longitudinal Self-Study of
Harness Engineering."*

논문 자체가 geworfen(*던져진 것*)의 물리적 실현이다 — 코드(뷰어) + 문서(논문) +
데이터(스냅샷)를 한 리포에서 재현한다. **이 리포가 논문의 재현 패키지다.**

작업 로그(SSOT): botlog `20260331T123550` (`~/org/botlog/`, 논문 워크로그).

### arXiv까지 남은 4가지

1. **Method + 최소실험(A/B, 5지표) 본문 이식** — 1층 차가운 실증. arXiv 방어선.
   재료는 botlog `[2026-04-03]` 헤딩에 이미 있다(조건 A 날것 / 조건 B 하네스 /
   범위일탈·경계준수·연속성회복·구조화품질·자기정당화감소). 코드가 아니라 이동.
2. **March → July 실증 갱신** — entwurf, pi-shell-acp, geworfen 진화(v2026.7.1).
   4월엔 하네스가 개념에 가까웠고, 지금은 실행체가 생긴 뒤의 논문이다.
3. **stale 수치 자동화** — `1,477→1,565`일, `3,300→3,561`노트, `2,100→2,248`가든.
   org export 매크로(`{{{notes-count}}}` 등)로 갱신.
4. **잔여** — `system diagram` / `sleep figure` 2개 TODO(→ `png/`), `geworfen.bib`
   cite 실체 + cite 경계 1건(geworfen.org:46 `---a`), ACM sigconf → arXiv `cs.HC`
   포맷 정렬, HTML 초록/저자 dual-target(현재 `#+BEGIN_EXPORT latex`라 HTML 손실).

## 기술 축 — 페이지 개선 = 표현 확장 = 증거 생성

geworfen 페이지를 키우는 일은 논문 5차원(리듬·연상·재현·투명·생체)의 증거를
만드는 일이다. 각 축이 어디에 증거를 대는지 함께 적는다.

### 축 1 — 관측성 / 신뢰성  → [투명]

- `/api/health`, 데몬 uptime·status endpoint, 죽으면 알림(Google Chat).
- `Restart=no`로 바꿔 "죽음이 진짜 신호"가 됐다(→ `ops/README.md`). geworfen이
  그 신호를 **드러내는** 표면이 되어야 한다.
- 가장 가깝고, 방금 겪은 사건의 자연스러운 연장.

### 축 2 — 시간축 확장  → [리듬 · longitudinal]

- `±14일` → 전체 히스토리. `diary.org`(18,954줄, 2022~) 통합, 과거로 걷기.
- 논문의 종단(longitudinal) 깊이가 곧 이 시간 표면이다.

### 축 3 — 데이터축 확장  → [생체 · 연상]

- agenda 너머 denote·bib·health·시간추적을 같은 표면에. "다양한 표현"의 핵심.
- 존재 데이터의 나머지 절반. 비전(README "존재 데이터 넥서스")의 종착.

### 축 4 — 아키텍처 진화  (축 2·3의 종속 결과)

- Option A → C/D. structured output(text→JSON), file-watch 자동 캐시 무효화.
- `ARCHITECTURE.md`의 "Future needs" 그대로.

## 대조 좌표 — 에이전트 확장 vs 인간 확장

Perplexity *"How AI Agents Reshape Knowledge Work"*(2026-06)와의 대조가 이
프로젝트의 좌표를 잡아준다.

| | Perplexity 논문 | geworfen / 하네스 |
|---|---|---|
| 관측 단위 | 제품 내부 세션·tool call | 가든·repo·journal·health·시간 증거 |
| 측정 대상 | agent-side expansion (26분 실행) | human-side expansion |
| 한 줄 | "더 많이 하게 됨" | "다른 인간이 되어 감" |

전자는 제품 로그로 측정되지만, 후자는 **시간축과 존재의 로그**가 있어야 겨우
보인다. geworfen은 그 후자를 보이게 만드는 표면이다.

## 관측 규율 — `junghan0611/timeline`을 본다. 건드리지 않는다

2026-07-13, 옆 리포에서 계측기가 하나 생겼다. `junghan0611/timeline` — git·note·
agenda를 KST 하나의 축으로 정규화하고 이벤트마다 `time_kind · domain · layer ·
ref`를 다는 수집기다. **연구를 위해 만든 것이 아니다.** 저쪽의 목적은 지금이고
(공개 프로파일·검증 관측소), 연구는 여기 목적이다. 그런데 결론은 여기서도 쓰게
된다 — 같은 사람의 시간축이므로.

**단방향이다. 여기서 저기로 요청·지시·수치 주문이 가지 않는다.** 이건 예의가
아니라 **데이터 무결성 조건**이다. 아래 두 방어가 그 위에 서 있고, 한 번만
새어도 둘 다 무너진다.

- **비반응적 측정 (non-reactive / unobtrusive).** 자기연구를 죽이는 첫째는
  반응성 — 측정된다는 걸 알면 데이터가 결론을 향해 휜다. 저 계측기는 채용
  실무자를 설득하려 최적화되고, 그 유인은 이 논문의 가설(인간이 시간축 위에서
  *의도적으로* 감각을 관리한다)과 **직교한다.** 다른 목적을 향해 벼려진 도구가
  우연히 내놓는 수치가, 이 가설을 위해 만든 도구의 수치보다 강하다.
- **사실상의 사전등록 (de facto preregistration).** 둘째는 HARKing — 결과를 보고
  가설을 짓는 것. 여기서는 순서가 공개 리포에 타임스탬프로 박혀 있다.
  논문 초안 `015cf15` (2026-04-01) → 계측기 `6476db1` (2026-07-13). **가설이
  도구보다 3개월 반 먼저다.** "데이터 보고 나서 이야기 지은 것 아니냐"에는
  `git log` 하나로 답한다.

이 접근의 이름을 적어 둔다. 도구 쪽은 **exaptation**(다른 기능으로 벼려진 것이
전용됨), 데이터 쪽은 **비반응적 측정**, 형식 전체는 **계측된 N-of-1 종단
자기연구** — HCI 문법으로는 *research through design*. 저쪽이 이쪽을 모르는
상태가 자기연구에서 거의 불가능한 **맹검**을 공짜로 만들어 준다.

### 논문이 저기서 가져올 넷 (관찰만 — 저쪽에 주문하지 않는다)

1. **깊이 축 0/1/2/3 = human-side expansion의 조작적 정의.** 깊이0(시간기록)·1
   (저널)은 인간이 *의도적으로 남긴* 기록이고, 깊이2(스탬프)·3(커밋·노트)은
   *잔여물*이다. 잔여물만 읽는 하네스는 무엇이 생산됐는지는 말해도 그것을
   생산한 삶은 못 말한다. 위 대조표의 "human-side"가 여기서 처음 측정 가능해진다.
2. **실패가 축에 남아 있다는 것 = cherry-picking 방어.** 자기연구가 리뷰어에게
   가장 먼저 맞는 공격이다. 롤백·revert·인시던트 커밋(openclaw 4.26 긴급 롤백,
   watchdog 자해 루프 16,354회, 26.05 GC 시한폭탄)이 그대로 있다는 사실이 이
   시간축이 연출되지 않았다는 증거다.
3. **두 기계가 다른 수를 낸다 = 재현 차원의 실증.** 같은 코드·같은 `--as-of`,
   oracle 16,524 / thinkpad 17,726. `unresolved_short_sha`는 결함이 아니라 이
   디스크에 그 커밋 객체가 없다는 정직한 경계다. **수치는 기계 라벨 없이 인용하지
   않는다.**
4. **타임로깅이 첫 커밋보다 2주 빠르다** (2021-10-31 vs 2021-11-17). 삶의 축이
   일의 축보다 먼저 있다.

**숫자 위생.** 폐기된 봉인 계층이 낸 수치(검증가능 32% / 봉인 68%)는 **무효**다.
논문에 흘러들면 안 된다. 그리고 손으로 유지하는 수는 이미 썩고 있다 — 글로벌
`AGENTS.md`의 노트 3,561·가든 2,248이 실제로는 3,572·2,245였다(2026-07-13 실측).
그러니 「arXiv까지 남은 4가지」의 3번(stale 수치 자동화)은 매크로가 아니라
저쪽 query 산출로 가는 것이 옳다.

**재현 패키지의 경계.** "이 리포가 논문의 재현 패키지"라는 위 문장은 이제
정확하지 않다. 계측기와 FULL 데이터는 저쪽에 있고 `events.jsonl`은 gitignore다.
공개되는 것은 snapshot manifest + query 코드 + 파생 집계이고 FULL은 로컬이다.
**이 사실을 논문에 그대로 쓴다** — 숨기고 "완전 재현 가능"이라 쓰는 순간 그것이
거짓이 되고, 밝히면 그 자체가 [투명] 차원의 증거가 된다.
